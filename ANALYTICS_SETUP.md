# 📊 Настройка аналитики

Этот шаблон поддерживает 3 системы аналитики, которые можно включить/выключить в `config.js`:

## 1. Facebook Pixel + Conversions API ✅

**Уже настроено и работает из коробки**

### Что отслеживается:
- ✅ Просмотры страницы (PageView)
- ✅ Клики по кнопкам WhatsApp/Telegram (Lead)
- ✅ Переходы на форму (Lead)
- ✅ Exit intent события
- ✅ Язык сайта (site_language) для разбивки по странам

### Настройка в config.js:

```javascript
analytics: {
  facebookPixel: {
    enabled: true,
    pixelId: "3789700971281396" // Замените на ваш Pixel ID
  }
}
```

### Где взять Pixel ID:
1. Откройте [Facebook Events Manager](https://business.facebook.com/events_manager2)
2. Создайте новый Dataset для вашей вакансии
3. Скопируйте Pixel ID

### Важно для Conversions API:
Добавьте в **Vercel Environment Variables**:
```
FB_PIXEL_ID=ваш_pixel_id
FB_DATASET_ID=ваш_dataset_id
FB_ACCESS_TOKEN=ваш_токен
```

Получить токен: `bash get-fb-token.sh`

---

## 2. Google Analytics 4 (GA4) 🆕

### Что отслеживается:
- Просмотры страницы
- События пользователя
- Источники трафика
- Поведение на сайте

### Настройка в config.js:

```javascript
analytics: {
  googleAnalytics: {
    enabled: true, // Изменить с false на true
    measurementId: "G-XXXXXXXXXX" // Замените на ваш Measurement ID
  }
}
```

### Как получить Measurement ID:

1. **Создайте аккаунт Google Analytics 4:**
   - Откройте [Google Analytics](https://analytics.google.com/)
   - Нажмите "Администратор" (шестеренка внизу слева)
   - Создайте новый **Ресурс** (Property)

2. **Настройте Web Stream:**
   - В ресурсе выберите "Потоки данных" (Data Streams)
   - Нажмите "Добавить поток" → "Веб"
   - Введите URL вашего сайта: `https://ce.woweri.com`
   - Нажмите "Создать поток"

3. **Скопируйте Measurement ID:**
   - В открывшемся окне найдите `Measurement ID`
   - Формат: `G-XXXXXXXXXX`
   - Скопируйте и вставьте в `config.js`

### Пример:

```javascript
analytics: {
  googleAnalytics: {
    enabled: true,
    measurementId: "G-ABC123XYZ" // Ваш реальный ID
  }
}
```

### Проверка работы:
1. Откройте [Google Analytics](https://analytics.google.com/)
2. Перейдите в **Отчеты → Реального времени**
3. Откройте ваш сайт в новой вкладке
4. Через 10-30 секунд увидите себя в отчете

---

## 3. Яндекс.Метрика 🆕

### Что отслеживается:
- Карта кликов (Clickmap)
- Вебвизор (запись сессий пользователей)
- Отслеживание ссылок
- Точный показатель отказов
- E-commerce события

### Настройка в config.js:

```javascript
analytics: {
  yandexMetrika: {
    enabled: true, // Изменить с false на true
    counterId: "XXXXXXXX" // Замените на ваш Counter ID
  }
}
```

### Как получить Counter ID:

1. **Создайте счётчик:**
   - Откройте [Яндекс.Метрика](https://metrika.yandex.ru/)
   - Войдите через Яндекс аккаунт
   - Нажмите **"Добавить счётчик"**

2. **Настройте счётчик:**
   - **Название:** "Лендинг Водители C+E" (или название вашей вакансии)
   - **Адрес сайта:** `https://ce.woweri.com`
   - **Часовой пояс:** Выберите ваш часовой пояс
   - Нажмите **"Создать счётчик"**

3. **Скопируйте Counter ID:**
   - После создания вы увидите номер счётчика (8-значное число)
   - Формат: `12345678`
   - Скопируйте и вставьте в `config.js`

### Пример:

```javascript
analytics: {
  yandexMetrika: {
    enabled: true,
    counterId: "98765432" // Ваш реальный Counter ID
  }
}
```

### Включите дополнительные функции:

В настройках счётчика включите:
- ✅ **Вебвизор** - запись сессий пользователей
- ✅ **Карта кликов** - визуализация кликов
- ✅ **Карта скроллинга** - как далеко пользователи прокручивают страницу
- ✅ **Формы** - отслеживание отправки форм

### Проверка работы:
1. Откройте [Яндекс.Метрика](https://metrika.yandex.ru/)
2. Выберите ваш счётчик
3. Перейдите в **"Посетители онлайн"**
4. Откройте ваш сайт в новой вкладке
5. Через 10-30 секунд увидите себя в списке

---

## 📋 Полный пример config.js с аналитикой:

```javascript
const CONFIG = {
  // ... остальные настройки ...

  analytics: {
    // Facebook Pixel (включен по умолчанию)
    facebookPixel: {
      enabled: true,
      pixelId: "3789700971281396"
    },

    // Google Analytics (включите после создания счётчика)
    googleAnalytics: {
      enabled: true,
      measurementId: "G-ABC123XYZ" // Ваш Measurement ID
    },

    // Яндекс.Метрика (включите после создания счётчика)
    yandexMetrika: {
      enabled: true,
      counterId: "98765432" // Ваш Counter ID
    }
  },

  // ... остальные настройки ...
};
```

---

## 🚀 После настройки:

### 1. Сгенерируйте лендинг:
```bash
node generate-landing.js
```

### 2. Проверьте локально:
Откройте `index.html` в браузере и убедитесь что:
- Нет ошибок в консоли (F12 → Console)
- Скрипты аналитики загрузились

### 3. Деплой на Vercel:
```bash
git add config.js index.html
git commit -m "Add Google Analytics and Yandex Metrika"
git push origin main
vercel --prod
```

---

## 🔍 Проверка работы всех систем:

### Facebook Pixel:
- Откройте [Facebook Events Manager](https://business.facebook.com/events_manager2)
- Выберите ваш Pixel
- Перейдите в "Test Events"
- Откройте ваш сайт
- Через 10-30 секунд увидите события

### Google Analytics:
- Откройте [Google Analytics](https://analytics.google.com/)
- Перейдите в "Отчеты → Реального времени"
- Откройте ваш сайт
- Увидите активного пользователя

### Яндекс.Метрика:
- Откройте [Яндекс.Метрика](https://metrika.yandex.ru/)
- Выберите счётчик
- Перейдите в "Посетители онлайн"
- Откройте ваш сайт
- Увидите себя в списке

---

## 💡 Советы:

### Для разных вакансий - разная аналитика:
Создавайте отдельные счётчики/пиксели для каждой вакансии:
- **Водители C+E:** GA4: `G-ABC123`, Метрика: `12345678`
- **Складские работники:** GA4: `G-DEF456`, Метрика: `87654321`
- **IT специалисты:** GA4: `G-GHI789`, Метрика: `11223344`

Это позволит точнее анализировать эффективность каждой вакансии.

### Отслеживание источников:
Используйте UTM параметры в ссылках для рекламы:
```
https://ce.woweri.com/?lang=uz&utm_source=facebook&utm_medium=cpc&utm_campaign=drivers_ce
```

### Цели и конверсии:
**Google Analytics:**
- Настройте "Конверсии" для кликов по WhatsApp/Telegram

**Яндекс.Метрика:**
- Настройте "Цели" для важных событий

**Facebook:**
- Используйте Custom Conversions для каждого языка

---

## 🆘 Troubleshooting:

### "Measurement ID not found" в Google Analytics
- Проверьте что Measurement ID начинается с `G-`
- Убедитесь что `enabled: true` в config.js
- Запустите `node generate-landing.js` заново

### "Counter ID invalid" в Яндекс.Метрика
- Counter ID должен быть числом без пробелов
- Проверьте что `enabled: true` в config.js
- Убедитесь что скопировали правильный ID из Метрики

### События не отображаются
- Подождите 10-60 секунд (может быть задержка)
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь что блокировщики рекламы отключены

---

**Готово! 🎉**

Теперь у вас настроена полная система аналитики для отслеживания эффективности лендинга.
